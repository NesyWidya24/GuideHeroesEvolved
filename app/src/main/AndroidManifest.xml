<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kagu.guideapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ChaosActivity" />
        <activity android:name=".ValleyActivity" />
        <activity android:name=".AltarActivity" />
        <activity android:name=".BannedRankActivity" />
        <activity android:name=".NormalRankActivity" />
        <activity android:name=".HeroesActivity" />
        <activity android:name=".ModesActivity" />
        <activity android:name=".GP4Activity" />
        <activity android:name=".GP3Activity" />
        <activity android:name=".GP2Activity" />
        <activity android:name=".GP1Activity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameplayActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>